version: '3.8'
services:
  nezha-telegram-bot:
    image: shentx/nezha-telegram-bot:latest
    container_name: nezha-telegram-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}